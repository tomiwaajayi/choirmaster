/**
 * @choirmaster/core - public API surface.
 *
 * Future barrels: state machine, gate runner, worktree manager, scope check.
 * For now, the type system is the contract; runtime mechanics land in
 * subsequent commits.
 */
export * from './types.js'
